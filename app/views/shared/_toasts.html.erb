<%# `toasts` is an array of { type:, title:, description: } — from the flash, or
    from a broadcast that replaces this element to pop a toast on a live page. %>
<div id="flash" class="hidden" data-controller="flash" data-flash-messages-value="<%= toasts.to_json %>"></div>
