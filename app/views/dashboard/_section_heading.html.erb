<%#
  One of the dashboard's three section sub-headings. Label size in Taupe,
  not accent: the Uppercase Ceiling Rule permits uppercase only as a quiet
  eyebrow, and in accent these read as loud as the footer's column labels.

  locals: title
%>
<h2 class="mb-4.5 text-t6 font-bold uppercase text-taupe"><%= title %></h2>
