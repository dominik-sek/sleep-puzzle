class TermsController < ApplicationController
  # Every clause comes from the CMS collection `terms.clauses`, so there is
  # nothing to load here - the same shape as AboutController.
  def show
  end
end
